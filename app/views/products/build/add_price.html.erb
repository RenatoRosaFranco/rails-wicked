<%= form_for(@product, { url: wizard_path, method: :put }) do |f| %>
  
  <div class="field">
    <%= f.label :price, 'Price' %>
    <%= f.number_field :price, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= f.submit 'Next' %>
  </div>
<% end %>